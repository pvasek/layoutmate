# Build output / DerivedData
build/
DerivedData/
.build/

# macOS
.DS_Store

# Xcode user-specific state (not the project structure itself, which is tracked)
xcuserdata/
*.xcuserstate
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xcscmblueprint
*.xccheckout

# Swift Package Manager (no SPM deps in this project, but excluded if anything generates them)
.swiftpm/
Packages/
Package.resolved
